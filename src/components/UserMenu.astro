<!-- Shell rendered server-side; hydrated by userClient.ts via /api/auth/me -->
<div id="user-menu" class="user-menu">

  <!-- Signed-out state -->
  <a href="/login" id="user-login-link" class="um-signin">Sign in</a>

  <!-- Signed-in state: email pill → hover dropdown -->
  <div id="user-loggedin" class="um-dropdown" hidden>
    <button type="button" class="um-trigger" aria-haspopup="true" aria-expanded="false">
      <span id="user-email" class="um-email"></span>
      <svg width="10" height="6" viewBox="0 0 10 6" fill="none" aria-hidden="true">
        <path d="M1 1l4 4 4-4" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/>
      </svg>
    </button>
    <div class="um-panel">
      <a href="/settings" class="um-item">Settings</a>
      <button type="button" id="user-logout" class="um-item">Sign out</button>
    </div>
  </div>

</div>

<style>
  .user-menu { display: flex; align-items: center; justify-content: flex-end; }

  /* Signed-out */
  .um-signin {
    font-size: 0.85rem;
    font-weight: 500;
    color: var(--muted);
    text-decoration: none;
  }
  .um-signin:hover { color: var(--accent); }

  /* Signed-in: trigger */
  .um-dropdown {
    position: relative;
    padding-bottom: 0.65rem; /* extends hover zone to bridge gap to panel */
  }
  .um-trigger {
    display: flex;
    align-items: center;
    gap: 0.35rem;
    background: none;
    border: none;
    padding: 0;
    cursor: pointer;
    font-family: inherit;
    font-size: 0.82rem;
    color: var(--muted);
    transition: color 0.15s;
  }
  .um-trigger:hover { color: var(--fg); }
  .um-email {
    max-width: 180px;
    overflow: hidden;
    text-overflow: ellipsis;
    white-space: nowrap;
  }

  /* Dropdown panel — shown on hover */
  .um-panel {
    display: none;
    position: absolute;
    top: 100%; /* padding-bottom on parent creates the visual gap */
    right: 0;
    background: var(--card);
    border: 1px solid var(--border);
    border-radius: 10px;
    padding: 0.35rem;
    min-width: 140px;
    box-shadow: 0 6px 20px rgba(0,0,0,0.09);
    z-index: 200;
  }
  .um-dropdown:hover .um-panel { display: block; }

  /* Dropdown items */
  .um-item {
    display: block;
    width: 100%;
    padding: 0.5rem 0.75rem;
    font-family: inherit;
    font-size: 0.85rem;
    font-weight: 500;
    color: var(--fg);
    text-decoration: none;
    background: none;
    border: none;
    border-radius: 7px;
    text-align: left;
    cursor: pointer;
    transition: background 0.12s, color 0.12s;
  }
  .um-item:hover { background: var(--bg); color: var(--accent); }
</style>
